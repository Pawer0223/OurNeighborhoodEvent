<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ptnInfos">

	<insert id="insertStoreInfo" parameterType = "first.actions.model.PtnInfos">
		<![CDATA[
		INSERT INTO PTN_INFOS
			SELECT 'P' || LPAD(MAX(SUBSTR(PTN_CD,2))+1,5,'0')
			, #{ bizrRegNo }
			, #{ ptnNm }
			, # { rpstNm }
			, # { rpstTel }
			, # { faxNo }
			, # { addNo }
			, # { neighbor }
			, SYSDATE,
			, ''
			FROM PTN_INFOS
		]]>	
	</insert>

</mapper>
