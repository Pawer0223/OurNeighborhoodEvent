<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userInfos">

	<select id="duplicateCheck" parameterType="String" resultType="java.lang.Integer">
		<![CDATA[	  
		SELECT 1 FROM USER_INFOS
		WHERE USER_ID=#{ inputId }
		]]>
	</select>
	
	<insert id="registUser" parameterType = "first.actions.model.UserInfos">
		<![CDATA[
		INSERT INTO USER_INFOS 
		VALUES ( #{ userId }
			    ,#{ userPw } 
			   	,#{ userNm }
			   	,#{ neighbor }
			   	,'N'
			   	,''
			   	,#{ email }
			   	,#{ profilePic }
			   	,#{ phoneNum }
			   	,''
			   	,0
			   	,''
			   	,''
			   	,SYSDATE
			   	,'' 
			   	)
		]]>	
	
	</insert>


</mapper>
